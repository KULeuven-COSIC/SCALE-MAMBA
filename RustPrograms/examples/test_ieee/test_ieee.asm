ldint r0, 1835104357
ldint r1, 1936026736
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 0
ldint r0, 1936028719
ldint r1, 1701404532
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 1
ldint r0, 1831822693
ldint r1, 778987873
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 2
ldint r0, 29554
ldint r1, 1048576
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 3
ldint r0, 26
ldint r1, 42
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 4
ldint r0, 28
ldint r1, 1048576
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 5
ldint r0, 26
ldint r1, 43
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 6
ldint r0, 29
ldint r1, 1048576
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 7
ldint r0, 26
ldint r1, 44
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 8
ldint r0, 28
ldint r1, 1048576
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 9
ldint r0, 26
ldint r1, 54
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 10
ldint r0, 19
ldint r1, 1048576
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 11
ldint r0, 26
ldint r1, 55
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 12
ldint r0, 19
ldint r1, 1048576
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 13
ldint r0, 26
ldint r1, 56
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 14
ldint r0, 19
ldint r1, 1048576
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 15
ldint r0, 26
ldint r1, 57
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 16
ldint r0, 19
ldint r1, 1048576
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 17
ldint r0, 26
ldint r1, 58
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 18
ldint r0, 19
ldint r1, 1048576
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 19
ldint r0, 26
ldint r1, 59
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 20
ldint r0, 19
ldint r1, 1048576
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 21
ldint r0, 26
ldint r1, 60
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 22
ldint r0, 19
ldint r1, 1048576
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 23
ldint r0, 26
ldint r1, 61
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 24
ldint r0, 19
ldint r1, 1048576
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 25
ldint r0, 26
ldint r1, 69
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 26
ldint r0, 18
ldint r1, 1048576
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 27
ldint r0, 26
ldint r1, 70
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 28
ldint r0, 18
ldint r1, 1048576
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 29
ldint r0, 26
ldint r1, 71
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 30
ldint r0, 18
ldint r1, 1048576
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 31
ldint r0, 26
ldint r1, 72
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 32
ldint r0, 18
ldint r1, 1048576
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 33
ldint r0, 26
ldint r1, 73
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 34
ldint r0, 18
ldint r1, 1048576
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 35
ldint r0, 26
ldint r1, 74
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
stmint r0, 36
ldint r0, 18
stmint r0, 37
ldint r0, -57999238
ldint r1, 1074340347
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
print_ieee_float r0 # converted function call directly to asm
print_char 10 # converted function call directly to asm
ldint r0, -57999238
ldint r1, 1074340347
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
convregsreg sr0, r0 # converted function call directly to asm
movsint sr1, sr0 # write to local 0
opensint r0, sr0 # converted function call directly to asm
print_ieee_float r0 # converted function call directly to asm
print_char 10 # converted function call directly to asm
ldint r0, 1677216
convregsreg sr0, r0 # converted function call directly to asm
movsint sr2, sr0 # write to local 2
pushsint sr0 # converted function call directly to asm
gc 125 # converted function call directly to asm
popsint sr0 # converted function call directly to asm
movsint sr3, sr0 # write to local 1
opensint r0, sr0 # converted function call directly to asm
print_ieee_float r0 # converted function call directly to asm
print_char 10 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
gc 124 # converted function call directly to asm
popsint sr0 # converted function call directly to asm
opensint r0, sr0 # converted function call directly to asm
print_int r0 # converted function call directly to asm
print_char 10 # converted function call directly to asm
opensint r0, sr2 # converted function call directly to asm
print_int r0 # converted function call directly to asm
print_char 10 # converted function call directly to asm
ldint r0, 2
convregsreg sr0, r0 # converted function call directly to asm
pushsint sr0 # converted function call directly to asm
gc 125 # converted function call directly to asm
popsint sr0 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
gc 120 # converted function call directly to asm
popsint sr2 # converted function call directly to asm
pushsint sr0 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
gc 121 # converted function call directly to asm
popsint sr4 # converted function call directly to asm
pushsint sr3 # converted function call directly to asm
pushsint sr0 # converted function call directly to asm
gc 122 # converted function call directly to asm
popsint sr5 # converted function call directly to asm
movsint sr0, sr5 # write to local 7
pushsint sr5 # converted function call directly to asm
pushsint sr0 # converted function call directly to asm
gc 120 # converted function call directly to asm
popsint sr0 # converted function call directly to asm
pushsint sr2 # converted function call directly to asm
pushsint sr2 # converted function call directly to asm
gc 121 # converted function call directly to asm
popsint sr5 # converted function call directly to asm
pushsint sr5 # converted function call directly to asm
gc 126 # converted function call directly to asm
popsint sr5 # converted function call directly to asm
ldint r0, 0
ldint r1, -2147483648
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
xorsintc sr4, sr4, r0 # converted function call directly to asm
pushsint sr2 # converted function call directly to asm
pushsint sr4 # converted function call directly to asm
gc 120 # converted function call directly to asm
popsint sr6 # converted function call directly to asm
opensint r0, sr6 # converted function call directly to asm
ldmint r1, 38
ldmint r2, 4
ldint r3, 32
shrint r2, r2, r3
addint r1, r1, r2
ldint r2, 1000
addint r1, r1, r2
stminti r0, r1 # converted function call directly to asm
ldint r0, 0
ldint r1, -2147483648
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
xorsintc sr4, sr3, r0 # converted function call directly to asm
pushsint sr0 # converted function call directly to asm
pushsint sr4 # converted function call directly to asm
gc 120 # converted function call directly to asm
popsint sr6 # converted function call directly to asm
opensint r0, sr6 # converted function call directly to asm
ldmint r1, 38
ldmint r2, 6
ldint r3, 32
shrint r2, r2, r3
addint r1, r1, r2
ldint r2, 1000
addint r1, r1, r2
stminti r0, r1 # converted function call directly to asm
ldint r0, 0
ldint r1, -2147483648
ldint r2, 65536
mulint r2, r2, r2
mulint r1, r1, r2
addint r0, r1, r0
xorsintc sr2, sr2, r0 # converted function call directly to asm
pushsint sr5 # converted function call directly to asm
pushsint sr2 # converted function call directly to asm
gc 120 # converted function call directly to asm
popsint sr0 # converted function call directly to asm
opensint r0, sr0 # converted function call directly to asm
ldmint r1, 38
ldmint r2, 8
ldint r3, 32
shrint r2, r2, r3
addint r1, r1, r2
ldint r2, 1000
addint r1, r1, r2
stminti r0, r1 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
pushsint sr3 # converted function call directly to asm
gc 127 # converted function call directly to asm
popsint sr4 # converted function call directly to asm
bitsint sb0, sr4, 0 # converted function call directly to asm
convsbitsint s0, sb0 # converted function call directly to asm
pushsint sr3 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
gc 127 # converted function call directly to asm
popsint sr6 # converted function call directly to asm
bitsint sb0, sr6, 0 # converted function call directly to asm
convsbitsint s1, sb0 # converted function call directly to asm
pushsint sr3 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
gc 123 # converted function call directly to asm
popsint sr5 # converted function call directly to asm
bitsint sb0, sr5, 0 # converted function call directly to asm
convsbitsint s2, sb0 # converted function call directly to asm
convsintsbit sb0, s1 # converted function call directly to asm
convsintsbit sb1, s2 # converted function call directly to asm
orsb sb0, sb0, sb1 # converted function call directly to asm
convsbitsint s1, sb0 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
gc 123 # converted function call directly to asm
popsint sr2 # converted function call directly to asm
bitsint sb0, sr2, 0 # converted function call directly to asm
convsbitsint s2, sb0 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
gc 123 # converted function call directly to asm
popsint sr0 # converted function call directly to asm
bitsint sb0, sr0, 0 # converted function call directly to asm
convsbitsint s3, sb0 # converted function call directly to asm
convsintsbit sb0, s3 # converted function call directly to asm
negb sb0, sb0 # converted function call directly to asm
convsbitsint s3, sb0 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
pushsint sr3 # converted function call directly to asm
gc 127 # converted function call directly to asm
popsint sr4 # converted function call directly to asm
bitsint sb0, sr4, 0 # converted function call directly to asm
convsbitsint s4, sb0 # converted function call directly to asm
pushsint sr3 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
gc 123 # converted function call directly to asm
popsint sr6 # converted function call directly to asm
bitsint sb0, sr6, 0 # converted function call directly to asm
convsbitsint s5, sb0 # converted function call directly to asm
convsintsbit sb0, s4 # converted function call directly to asm
convsintsbit sb1, s5 # converted function call directly to asm
orsb sb0, sb0, sb1 # converted function call directly to asm
convsbitsint s4, sb0 # converted function call directly to asm
pushsint sr3 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
gc 127 # converted function call directly to asm
popsint sr5 # converted function call directly to asm
bitsint sb0, sr5, 0 # converted function call directly to asm
convsbitsint s5, sb0 # converted function call directly to asm
pushsint sr3 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
gc 123 # converted function call directly to asm
popsint sr2 # converted function call directly to asm
bitsint sb0, sr2, 0 # converted function call directly to asm
convsbitsint s6, sb0 # converted function call directly to asm
convsintsbit sb0, s5 # converted function call directly to asm
convsintsbit sb1, s6 # converted function call directly to asm
orsb sb0, sb0, sb1 # converted function call directly to asm
convsbitsint s5, sb0 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
gc 127 # converted function call directly to asm
popsint sr0 # converted function call directly to asm
bitsint sb0, sr0, 0 # converted function call directly to asm
convsbitsint s6, sb0 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
gc 123 # converted function call directly to asm
popsint sr4 # converted function call directly to asm
bitsint sb0, sr4, 0 # converted function call directly to asm
convsbitsint s7, sb0 # converted function call directly to asm
convsintsbit sb0, s6 # converted function call directly to asm
convsintsbit sb1, s7 # converted function call directly to asm
orsb sb0, sb0, sb1 # converted function call directly to asm
convsbitsint s6, sb0 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
gc 127 # converted function call directly to asm
popsint sr6 # converted function call directly to asm
bitsint sb0, sr6, 0 # converted function call directly to asm
convsbitsint s7, sb0 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
gc 123 # converted function call directly to asm
popsint sr5 # converted function call directly to asm
bitsint sb0, sr5, 0 # converted function call directly to asm
convsbitsint s8, sb0 # converted function call directly to asm
convsintsbit sb0, s7 # converted function call directly to asm
convsintsbit sb1, s8 # converted function call directly to asm
orsb sb0, sb0, sb1 # converted function call directly to asm
convsbitsint s7, sb0 # converted function call directly to asm
convsintsbit sb0, s0 # converted function call directly to asm
opensbit r0, sb0 # converted function call directly to asm
ldmint r1, 38
ldmint r2, 10
ldint r3, 32
shrint r2, r2, r3
addint r1, r1, r2
ldint r2, 1000
addint r1, r1, r2
stminti r0, r1 # converted function call directly to asm
convsintsbit sb0, s1 # converted function call directly to asm
opensbit r0, sb0 # converted function call directly to asm
ldmint r1, 38
ldmint r2, 12
ldint r3, 32
shrint r2, r2, r3
addint r1, r1, r2
ldint r2, 1000
addint r1, r1, r2
stminti r0, r1 # converted function call directly to asm
convsintsbit sb0, s2 # converted function call directly to asm
opensbit r0, sb0 # converted function call directly to asm
ldmint r1, 38
ldmint r2, 14
ldint r3, 32
shrint r2, r2, r3
addint r1, r1, r2
ldint r2, 1000
addint r1, r1, r2
stminti r0, r1 # converted function call directly to asm
convsintsbit sb0, s3 # converted function call directly to asm
opensbit r0, sb0 # converted function call directly to asm
ldmint r1, 38
ldmint r2, 16
ldint r3, 32
shrint r2, r2, r3
addint r1, r1, r2
ldint r2, 1000
addint r1, r1, r2
stminti r0, r1 # converted function call directly to asm
convsintsbit sb0, s4 # converted function call directly to asm
opensbit r0, sb0 # converted function call directly to asm
ldmint r1, 38
ldmint r2, 18
ldint r3, 32
shrint r2, r2, r3
addint r1, r1, r2
ldint r2, 1000
addint r1, r1, r2
stminti r0, r1 # converted function call directly to asm
convsintsbit sb0, s5 # converted function call directly to asm
opensbit r0, sb0 # converted function call directly to asm
ldmint r1, 38
ldmint r2, 20
ldint r3, 32
shrint r2, r2, r3
addint r1, r1, r2
ldint r2, 1000
addint r1, r1, r2
stminti r0, r1 # converted function call directly to asm
convsintsbit sb0, s6 # converted function call directly to asm
opensbit r0, sb0 # converted function call directly to asm
ldmint r1, 38
ldmint r2, 22
ldint r3, 32
shrint r2, r2, r3
addint r1, r1, r2
ldint r2, 1000
addint r1, r1, r2
stminti r0, r1 # converted function call directly to asm
convsintsbit sb0, s7 # converted function call directly to asm
opensbit r0, sb0 # converted function call directly to asm
ldmint r1, 38
ldmint r2, 24
ldint r3, 32
shrint r2, r2, r3
addint r1, r1, r2
ldint r2, 1000
addint r1, r1, r2
stminti r0, r1 # converted function call directly to asm
ldint r0, 0
convregsreg sr3, r0 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
pushsint sr3 # converted function call directly to asm
gc 127 # converted function call directly to asm
popsint sr2 # converted function call directly to asm
bitsint sb0, sr2, 0 # converted function call directly to asm
convsbitsint s0, sb0 # converted function call directly to asm
ldint r0, 0
convregsreg sr3, r0 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
pushsint sr3 # converted function call directly to asm
gc 127 # converted function call directly to asm
popsint sr0 # converted function call directly to asm
bitsint sb0, sr0, 0 # converted function call directly to asm
convsbitsint s1, sb0 # converted function call directly to asm
ldint r0, 0
convregsreg sr3, r0 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
pushsint sr3 # converted function call directly to asm
gc 123 # converted function call directly to asm
popsint sr4 # converted function call directly to asm
bitsint sb0, sr4, 0 # converted function call directly to asm
convsbitsint s2, sb0 # converted function call directly to asm
convsintsbit sb0, s1 # converted function call directly to asm
convsintsbit sb1, s2 # converted function call directly to asm
orsb sb0, sb0, sb1 # converted function call directly to asm
convsbitsint s1, sb0 # converted function call directly to asm
ldint r0, 0
convregsreg sr6, r0 # converted function call directly to asm
pushsint sr6 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
gc 127 # converted function call directly to asm
popsint sr5 # converted function call directly to asm
bitsint sb0, sr5, 0 # converted function call directly to asm
convsbitsint s2, sb0 # converted function call directly to asm
ldint r0, 0
convregsreg sr2, r0 # converted function call directly to asm
pushsint sr2 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
gc 127 # converted function call directly to asm
popsint sr0 # converted function call directly to asm
bitsint sb0, sr0, 0 # converted function call directly to asm
convsbitsint s3, sb0 # converted function call directly to asm
ldint r0, 0
convregsreg sr3, r0 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
pushsint sr3 # converted function call directly to asm
gc 123 # converted function call directly to asm
popsint sr4 # converted function call directly to asm
bitsint sb0, sr4, 0 # converted function call directly to asm
convsbitsint s4, sb0 # converted function call directly to asm
convsintsbit sb0, s3 # converted function call directly to asm
convsintsbit sb1, s4 # converted function call directly to asm
orsb sb0, sb0, sb1 # converted function call directly to asm
convsbitsint s3, sb0 # converted function call directly to asm
ldint r0, 0
convregsreg sr3, r0 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
pushsint sr3 # converted function call directly to asm
gc 123 # converted function call directly to asm
popsint sr6 # converted function call directly to asm
bitsint sb0, sr6, 0 # converted function call directly to asm
convsbitsint s4, sb0 # converted function call directly to asm
ldint r0, 0
convregsreg sr3, r0 # converted function call directly to asm
pushsint sr1 # converted function call directly to asm
pushsint sr3 # converted function call directly to asm
gc 123 # converted function call directly to asm
popsint sr5 # converted function call directly to asm
bitsint sb0, sr5, 0 # converted function call directly to asm
convsbitsint s8, sb0 # converted function call directly to asm
convsintsbit sb0, s8 # converted function call directly to asm
negb sb0, sb0 # converted function call directly to asm
convsbitsint s5, sb0 # converted function call directly to asm
convsintsbit sb0, s0 # converted function call directly to asm
opensbit r0, sb0 # converted function call directly to asm
ldmint r1, 38
ldmint r2, 26
ldint r3, 32
shrint r2, r2, r3
addint r1, r1, r2
ldint r2, 1000
addint r1, r1, r2
stminti r0, r1 # converted function call directly to asm
convsintsbit sb0, s1 # converted function call directly to asm
opensbit r0, sb0 # converted function call directly to asm
ldmint r1, 38
ldmint r2, 28
ldint r3, 32
shrint r2, r2, r3
addint r1, r1, r2
ldint r2, 1000
addint r1, r1, r2
stminti r0, r1 # converted function call directly to asm
convsintsbit sb0, s2 # converted function call directly to asm
opensbit r0, sb0 # converted function call directly to asm
ldmint r1, 38
ldmint r2, 30
ldint r3, 32
shrint r2, r2, r3
addint r1, r1, r2
ldint r2, 1000
addint r1, r1, r2
stminti r0, r1 # converted function call directly to asm
convsintsbit sb0, s3 # converted function call directly to asm
opensbit r0, sb0 # converted function call directly to asm
ldmint r1, 38
ldmint r2, 32
ldint r3, 32
shrint r2, r2, r3
addint r1, r1, r2
ldint r2, 1000
addint r1, r1, r2
stminti r0, r1 # converted function call directly to asm
convsintsbit sb0, s4 # converted function call directly to asm
opensbit r0, sb0 # converted function call directly to asm
ldmint r1, 38
ldmint r2, 34
ldint r3, 32
shrint r2, r2, r3
addint r1, r1, r2
ldint r2, 1000
addint r1, r1, r2
stminti r0, r1 # converted function call directly to asm
convsintsbit sb0, s5 # converted function call directly to asm
opensbit r0, sb0 # converted function call directly to asm
ldmint r1, 38
ldmint r2, 36
ldint r3, 32
shrint r2, r2, r3
addint r1, r1, r2
ldint r2, 1000
addint r1, r1, r2
stminti r0, r1 # converted function call directly to asm
