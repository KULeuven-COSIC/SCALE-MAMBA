zero = regint(0)
shared_one = sint(1)
# Arguments = location in memory
mem_two = load_clear_mem(2)
shared_mem_three = load_secret_mem(3)
const_mem_zero = load_clear_mem(zero)
shared_mem_zero = load_secret_mem(zero)
# Stores zero to location 0
store_in_mem(zero, 0)
store_in_mem(shared_one, 0)
store_in_mem(zero, zero)
store_in_mem(shared_one, zero)
one = reveal(shared_one)
# x = get_input_from(0)
# t1, t2, t3 = get_random_triple()
# b = get_random_bit()
# s1, s2 = get_random_square()
# i1, i2 = get_random_inverse()
