Instructions to create the pushed version to github

1) create a new folder with SCALE;

2) Remove .git folder

3) git init; then: git remote add origin
git@github.com:username/reponame.git

4) git pull ; git add * ; git commit ; git push origin master


